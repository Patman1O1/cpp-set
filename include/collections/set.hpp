#ifndef COLLECTIONS_SET_HPP
#define COLLECTIONS_SET_HPP

// ISO C Includes
#include <cstddef>

// ISO C++ Includes
#include <ranges>

namespace collections {
    template<typename T, std::ranges::range Container>
    class set {
    
    };
} // namespace collections

#endif // #ifndef COLLECTIONS_SET_HPP
